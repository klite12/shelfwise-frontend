// ==========================================================================
// Shelfwise — front-end configuration
// ==========================================================================
// Points the static frontend at the live Django backend on Render.
// If you ever move the backend, this is the only line you need to change.

window.SHELFWISE_CONFIG = {
  API_BASE_URL: 'https://shelfwise-admin.onrender.com',
};
